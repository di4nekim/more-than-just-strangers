# Firebase Integration Test Environment Variables
# Copy this file to .env.test and update with your test values

# === REQUIRED FOR FIREBASE INTEGRATION TESTS ===

# Firebase Configuration
FIREBASE_PROJECT_ID=your-firebase-project-id

# Firebase Service Account Credentials (for server-side tests)
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour private key here\n-----END PRIVATE KEY-----\n"
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project.iam.gserviceaccount.com

# Client-side Firebase Configuration
NEXT_PUBLIC_FIREBASE_API_KEY=your-firebase-api-key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-firebase-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789012
NEXT_PUBLIC_FIREBASE_APP_ID=1:123456789012:web:abcdef1234567890

# Test User Credentials (will be created if they don't exist)
TEST_USER_EMAIL=test+firebase@yourdomain.com
TEST_USER_PASSWORD=TestPassword123!

# === OPTIONAL CONFIGURATION ===

# Firebase Auth Emulator (for local development)
FIREBASE_AUTH_EMULATOR_HOST=localhost:9099

# WebSocket API URL for WebSocket auth tests
NEXT_PUBLIC_WEBSOCKET_API_URL=wss://your-websocket-api.execute-api.us-east-1.amazonaws.com/dev

# Application URLs
NEXTAUTH_URL=http://localhost:3000
ALLOWED_ORIGINS=http://localhost:3000,https://localhost:3000

# === TEST CONFIGURATION ===

# Set to 'true' to enable integration tests that require real Firebase resources
ENABLE_INTEGRATION_TESTS=false

# Set to 'true' to enable tests that create/delete Firebase users
ENABLE_DESTRUCTIVE_TESTS=false

# Test environment (test/staging/local)
TEST_ENVIRONMENT=test

# Timeout for integration tests (milliseconds)
INTEGRATION_TEST_TIMEOUT=30000

# === SECURITY NOTES ===
# 
# 1. NEVER commit this file with real credentials
# 2. Use Firebase service account with minimal permissions for testing
# 3. Use separate Firebase project for testing, not production
# 4. Consider using Firebase Auth Emulator for local testing
# 5. Test users will be created and cleaned up automatically
#
# === MINIMAL FIREBASE PERMISSIONS REQUIRED ===
#
# The service account should have the following roles:
# - Firebase Admin SDK Administrator Service Agent
# - Service Account Token Creator
#
# For testing, you can also use the Firebase Auth Emulator:
# firebase emulators:start --only auth 